package com.wings.mile.mapbox

import android.os.Bundle
import androidx.appcompat.app.AppCompatActivity
import androidx.lifecycle.lifecycleScope
import com.wings.mile.R

class Addresslocation : AppCompatActivity() {
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        //val addressAutofill = AddressAutofill.create(R.string.mapbox_access_token.toString())

//        val query = receivedInput
//        lifecycleScope.launchWhenCreated {
//            val response = addressAutofill.suggestions(query, AddressAutofillOptions())
//            when (response) {
//                is AddressAutofillResponse.Suggestions -> {
//                    showSuggestions(response.suggestions)
//                }
//
//
//            }
        }

    //}
}